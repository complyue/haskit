
import ( HaskItServer, **_ ) 'haskit'


hskiServer = HaskItServer(
  '127.0.0.1', 3780, 3790,
)


# %%
# this line helps to keep the server live when run non-interactively
# also it ungrabs the console, so REPLs can exclusively use the console
# for debug purpose
hskiServer.join()
